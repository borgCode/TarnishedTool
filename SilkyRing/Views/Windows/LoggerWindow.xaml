<Window x:Class="SilkyRing.Views.LoggerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Style="{StaticResource DefaultWindowStyle}"
        WindowStyle="None"
        AllowsTransparency="True"
        mc:Ignorable="d"
        Title="Game Logger" Height="900" Width="900">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
                Background="{StaticResource TitleBarBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Image Source="/Assets/Silky.ico"
                       Grid.Column="0"
                       Width="16"
                       Height="16"
                       Margin="5,0,5,0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center" />

                <TextBlock Text="Logger"
                           Grid.Column="1"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}" />

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="—"
                            Width="46"
                            Height="30"
                            Click="MinimizeButton_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="✕"
                            Width="46"
                            Height="30"
                            Click="CloseButton_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E81123" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Padding="10">
                <StackPanel>


                    <WrapPanel Orientation="Horizontal">
                        <CheckBox Content="Set event" IsChecked="{Binding IsSetEventLogging}" Margin="0,0,15,5" />
                        <CheckBox Content="Set event once per ID" IsChecked="{Binding IsSetEventUniqueLogging}"
                                  Margin="0,0,15,5" />
                        <CheckBox Content="Set special effect" IsChecked="{Binding IsApplySpEffectLogging}"
                                  Margin="0,0,15,5" />
                        <CheckBox Content="Set special effect once per ID"
                                  IsChecked="{Binding IsApplySpEffectUniqueLogging}"
                                  Margin="0,0,15,5" />
                        <CheckBox Content="AI Goal creation" IsChecked="{Binding IsAiGoalsLogging}" Margin="0,0,15,5" />
                    </WrapPanel>


                    <WrapPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Clear Unique Events" Click="ClearUniqueSetEvents_Click"
                                Padding="10,5" Margin="0,0,10,0" />
                        <Button Content="Clear Unique SpEffects" Click="ClearUniqueSpEffects_Click"
                                Padding="10,5" Margin="0,0,10,0" />
                        <Button Content="Clear Console" Click="ClearConsole_Click"
                                Padding="10,5" Margin="0,0,10,0" />
                        <Button Content="Pause All Logging" Click="PauseAllLogging_Click"
                                Padding="10,5" Margin="0,0,10,0" />
                    </WrapPanel>


                </StackPanel>
            </Border>

            <Border Grid.Row="1" Margin="10">
                <Grid>
                    <Border
                        Visibility="{Binding IsInitializing, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10" />
                            <TextBlock Text="Initializing logger..." HorizontalAlignment="Center" FontSize="14" />
                        </StackPanel>
                    </Border>

                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <TextBox Text="{Binding LogText}"
                                 IsReadOnly="True"
                                 Background="Black"
                                 Foreground="Lime"
                                 FontFamily="Consolas"
                                 FontSize="12"
                                 TextWrapping="NoWrap"
                                 BorderThickness="0"
                                 Padding="5" />
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>