<Window x:Class="SilkyRing.Views.Windows.DefensesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:controls="clr-namespace:SilkyRing.Controls"
        Style="{StaticResource DefaultWindowStyle}"
        WindowStyle="None"
        AllowsTransparency="True"
        mc:Ignorable="d"
        
        Title="DefenseWindow" Height="275" Width="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:TitleBar Title="Defenses" />

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="Margin" Value="0,5,0,0" />
                </Style>
            </Grid.Resources>


            <TextBlock Grid.Row="0" Grid.Column="0" Text="Standard:" FontSize="14" Margin="0,0,0,5" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding StandardDefense, StringFormat={}{0:F1}%}" FontSize="14" Margin="0,0,0,5" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Slash:" FontSize="14" Margin="0,0,0,5" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SlashDefense, StringFormat={}{0:F1}%}" FontSize="14" Margin="0,0,0,5" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Strike:" FontSize="14" Margin="0,0,0,5" />
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding StrikeDefense, StringFormat={}{0:F1}%}" FontSize="14" Margin="0,0,0,5" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Thrust:" FontSize="14" Margin="0,0,0,5" />
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ThrustDefense, StringFormat={}{0:F1}%}" FontSize="14" Margin="0,0,0,5" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Magic:" FontSize="14" Margin="0,0,0,5" />
            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding MagicDefense, StringFormat={}{0:F1}%}" FontSize="14" Margin="0,0,0,5" />

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Fire:" FontSize="14" Margin="0,0,0,5" />
            <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding FireDefense, StringFormat={}{0:F1}%}" FontSize="14" Margin="0,0,0,5" />

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Lightning:" FontSize="14" Margin="0,0,0,5" />
            <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding LightningDefense, StringFormat={}{0:F1}%}" FontSize="14" Margin="0,0,0,5" />

            <TextBlock Grid.Row="7" Grid.Column="0" Text="Holy:" FontSize="14" />
            <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding HolyDefense, StringFormat={}{0:F1}%}" FontSize="14" />
            
        </Grid>
    </Grid>
</Window>
